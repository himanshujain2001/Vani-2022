  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <!-- App Name or Logo -->
      <a href="/" class="navbar-brand d-flex align-items-center mb-3 mb-md-0 me-md-auto text-decoration-none">
        <img src="/images/tsp-174-modified.png" alt="Logo" width="40" height="40">
      </a>

      <!-- User Profile Dropdown -->
      <div class="dropdown ms-auto"> <!-- Add `ms-auto` to float it to the right -->
        <!-- Profile Toggle -->
        <a
          class="d-flex align-items-center text-white text-decoration-none dropdown-toggle"
          id="userDropdown"
          role="button"
          data-bs-toggle="dropdown"
          aria-expanded="false"
          href="#"
        >
          <!-- User Profile Image -->
          <img
            src="https://ui-avatars.com/api/?name=Vani&size=40&background=<%= color %>&color=ffffff"
            alt="Profile"
            class="rounded-circle me-1"
            width="40"
            height="40"
          />
        </a>
        <!-- Dropdown Items -->
        <ul
          class="dropdown-menu dropdown-menu-end text-small"
          aria-labelledby="userDropdown"
        >
          <!-- <li><a class="dropdown-item" href="<%= basePath %>/">Home</a></li> -->
          <li><a class="dropdown-item" href="<%= basePath %>/gallery">Gallery</a></li>
          <li><a class="dropdown-item" href="<%= basePath %>/contact">Contact</a></li>
          <li><hr class="dropdown-divider"/></li>
          <li>
           <form action="<%= basePath %>/logout" method="POST" style="display: inline;">
             <button type="submit" class="dropdown-item btn btn-link">Sign Out</button>
           </form>
          </li>
        </ul>
      </div>
    </div>
  </nav>